node_modules
out
dist
dist-electron
release
*.log
.DS_Store
*.tsbuildinfo

# Live status board scratch (see scripts/live.sh) — never committed.
.live-start
.live-tests
.live-log

# Local audit exports and recovery archives — never commit these.
quarantine-prep/
output.json
history_aiza.txt
docs/Local_State_Report.txt
docs/Target_State_Report.txt
